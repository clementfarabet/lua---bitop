CMAKE_MINIMUM_REQUIRED(VERSION 2.6 FATAL_ERROR)
CMAKE_POLICY(VERSION 2.6)
FIND_PACKAGE(Torch REQUIRED)

SET(src bit.c)
SET(luasrc init.lua)

ADD_TORCH_PACKAGE(bit "${src}" "${luasrc}" "System")
TARGET_LINK_LIBRARIES(bit luaT TH)
